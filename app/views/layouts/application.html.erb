<!DOCTYPE html>
<html>
  <head>
    <title>Rails Template</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-css-only@4.4/css/bootstrap.min.css" integrity="sha256-L/W5Wfqfa0sdBNIKN9cG6QA5F2qx4qICmU2VgLruv9Y=" crossorigin="anonymous">
  </head>

  <body>
    <nav>
      <% if current_user != nil %>
        <a href="/users/sign_out">Sign Out</a>
        <a href="/users/edit_profile">Edit Profile</a>
      <% else %>
        <a href="/users/sign_up">Sign Up</a>
        <a href="/users/sign_in">Sign in</a>
      <% end %>
    </nav>

    <% if notice.present? %>
      <div style="color: green">
        <%= notice%>
      </div>
    <% end %>

    <% if alert.present? %>
      <div style="color: red">
        <%= notice%>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
